# 前端依赖（保留在仓库根，供 frontend 向上解析）
node_modules

# 前端构建产物
frontend/dist

# Rust 后端构建产物
server/target

# 本地开发
*.local

# 编辑器 / 系统
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# 日志
logs
*.log
npm-debug.log*

# 配置库（SQLCipher 加密的 SQLite，含口令相关元数据，严禁提交）
server/config.db
server/config.db-journal
server/config.db-wal
server/config.db-shm
ToDo.md
